class_name: AcceptanceTester
modules:
    enabled: [AcceptanceHelper, WebDriver, Laravel5]
    config:
      WebDriver:
        url: 'http://localhost:8000'
        browser: phantomjs
        capabilities:
          webStorageEnabled: true
      Laravel5:
        environment_file: .env.testing
